import{useEffect,useState}from'react';import{get}from'../lib/api';export default function Data({kind}:{kind:string}){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{const m:any={contracts:'/tables/contracts',executions:'/tables/executions',realized:'/realized',charges:'/charges',missing:'/missing-dates',securities:'/securities',holdings:'/holdings'};get<any[]>(m[kind]||'/dashboard').then(setRows)},[kind]);const cols=rows[0]?Object.keys(rows[0]):[];return <div className="page"><div className="eyebrow">DATA VIEW</div><h1>{kind[0].toUpperCase()+kind.slice(1)}</h1><div className="card"><div className="table-wrap"><table><thead><tr>{cols.map(c=><th key={c}>{c.replaceAll('_',' ')}</th>)}</tr></thead><tbody>{rows.slice(0,1000).map((r,i)=><tr key={i}>{cols.map(c=><td key={c}>{typeof r[c]==='number'?r[c].toLocaleString():String(r[c]??'')}</td>)}</tr>)}</tbody></table></div></div></div>}
