import {useEffect,useState} from 'react';import {get,downloadDataset} from '../lib/api';export default function Data({title,endpoint,exportKey}:{title:string,endpoint:string,exportKey:string}){const [d,setD]=useState<any>(null);useEffect(()=>{get(endpoint).then(setD).catch(()=>setD({}))},[endpoint]);return <section><div className="page-head"><div><h1>{title}</h1></div><button onClick={()=>downloadDataset(exportKey)}>Export</button></div><pre className="raw">{d?JSON.stringify(d,null,2):'Loading…'}</pre></section>}
