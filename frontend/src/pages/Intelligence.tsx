import{useEffect,useState}from'react';import{get}from'../lib/api';import{LineChart,Line,CartesianGrid,XAxis,YAxis,Tooltip,ResponsiveContainer,BarChart,Bar}from'recharts';export default function Intelligence(){const[d,setD]=useState<any>();useEffect(()=>{get('/intelligence').then(setD)},[]);if(!d)return <div className="page">Loading…</div>;const m=d.metrics||{};return <div className="page"><div className="eyebrow">RISK & INTELLIGENCE</div><h1>Portfolio Intelligence</h1><p className="muted">Risk diagnostics, concentration warnings and performance efficiency.</p><div className="grid kpis"><div className="kpi"><div className="eyebrow">MAX DRAWDOWN</div><div className="value">₹{(m.max_drawdown||0).toLocaleString()}</div></div><div className="kpi"><div className="eyebrow">VOLATILITY</div><div className="value">{m.volatility==null?'—':m.volatility.toFixed(2)}</div></div><div className="kpi"><div className="eyebrow">SHARPE-LIKE</div><div className="value">{m.sharpe_like==null?'—':m.sharpe_like.toFixed(2)}</div></div><div className="kpi"><div className="eyebrow">CVaR 95%</div><div className="value">₹{m.cvar_95==null?'—':m.cvar_95.toLocaleString()}</div></div></div><div className="grid two"><section className="card"><h2>Daily realized P&L</h2><ResponsiveContainer width="100%" height={280}><LineChart data={d.daily_pnl}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip/><Line type="monotone" dataKey="pnl" strokeWidth={2}/></LineChart></ResponsiveContainer></section><section className="card"><h2>Risk alerts</h2>{d.alerts.length===0?<p className="muted">No threshold alerts.</p>:d.alerts.map((a:any,i:number)=><div className="alert" key={i}><strong>{a.severity}</strong><div><b>{a.title}</b><div className="muted">{a.detail}</div></div></div>)}</section></div><div className="card"><h2>Position risk map</h2><ResponsiveContainer width="100%" height={300}><BarChart data={d.holdings.map((x:any)=>({name:x.security.slice(0,18),weight:+(x.weight*100).toFixed(1)}))}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="weight"/></BarChart></ResponsiveContainer></div></div>}
