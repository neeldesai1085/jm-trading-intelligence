import {useState} from 'react';import {register} from '../lib/api';export default function Register({onAuthenticated}:{onAuthenticated:(u:any)=>void}){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');return <div className="auth-shell"><form className="auth-card" onSubmit={async e=>{e.preventDefault();setError('');try{onAuthenticated(await register(name,email,password))}catch(err:any){setError(err.message)}}}><div className="eyebrow">JM TRADING INTELLIGENCE</div><h1>Create account</h1><input placeholder="Name" value={name} onChange={e=>setName(e.target.value)} required/><input placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/><button>Create account</button>{error&&<p className="error">{error}</p>}</form></div>}
