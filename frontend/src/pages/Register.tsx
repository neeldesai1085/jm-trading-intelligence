import {useState} from 'react';
import {register} from '../lib/api';
export default function Register({onAuthenticated}:{onAuthenticated:(u:any)=>void}){const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[err,setErr]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();if(password!==confirm){setErr('Passwords do not match');return}setBusy(true);setErr('');try{const r=await register(name,email,password);onAuthenticated(r.user)}catch(x:any){setErr(x.message||'Registration failed')}finally{setBusy(false)}};return <div className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">JM TRADING INTELLIGENCE</div><h1>Create account</h1><p className="muted">Your trading history is isolated to your account.</p><label>Name<input autoComplete="name" value={name} onChange={e=>setName(e.target.value)} required maxLength={120}/></label><label>Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label><label>Confirm password<input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>{err&&<div className="alert high">{err}</div>}<button className="primary" disabled={busy}>{busy?'Creating…':'Create account'}</button><div className="auth-switch">Already registered? <button type="button" onClick={()=>onAuthenticated({showLogin:true})}>Sign in</button></div></form></div>}
