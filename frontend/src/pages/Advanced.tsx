import {useEffect,useState} from 'react';import {get} from '../lib/api';import {BarChart,Bar,CartesianGrid,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';export default function Advanced(){const[d,setD]=useState<any>();const[e,setE]=useState('');useEffect(()=>{get('/analytics/advanced').then(setD).catch((x:any)=>setE(x.message))},[]);if(e)return <div className="page"><div className="card">{e}</div></div>;if(!d)return <div className="page">Loading…</div>;const q=d.quote_staleness||[];return <div className="page"><div className="eyebrow">ADVANCED ANALYTICS</div><h1>Trader Quality & Stress Lab</h1><p className="muted">Expectancy, payoff, streaks, live-quote health and downside stress scenarios.</p><div className="grid kpis"><div className="kpi"><div className="eyebrow">EXPECTANCY / TRADE</div><div className="value">₹{(d.trade_quality.expectancy_per_trade||0).toLocaleString()}</div></div><div className="kpi"><div className="eyebrow">PROFIT FACTOR</div><div className="value">{d.trade_quality.profit_factor==null?'—':d.trade_quality.profit_factor.toFixed(2)}×</div></div><div className="kpi"><div className="eyebrow">MAX WIN STREAK</div><div className="value">{d.trade_quality.max_consecutive_wins}</div></div><div className="kpi"><div className="eyebrow">MAX LOSS STREAK</div><div className="value">{d.trade_quality.max_consecutive_losses}</div></div></div><div className="card"><h2>Benchmark readiness</h2><p>{d.benchmark?.configured?`Configured benchmark snapshots: ${d.benchmark.points}. ${d.benchmark.message||''}`:'No benchmark configured. Set BENCHMARK_ISIN and BENCHMARK_INSTRUMENT_KEY to enable benchmark tracking.'}</p></div><div className="grid two"><section className="card"><h2>Open-book stress</h2><ResponsiveContainer width="100%" height={280}><BarChart data={d.stress_tests}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="shock_pct"/><YAxis/><Tooltip/><Bar dataKey="estimated_pnl"/></BarChart></ResponsiveContainer></section><section className="card"><h2>Quote freshness</h2><div className="table-wrap"><table><thead><tr><th>Security</th><th>Weight</th><th>Quote age</th><th>Unrealized</th></tr></thead><tbody>{q.map((x:any)=><tr key={x.security}><td>{x.security}</td><td>{(x.weight*100).toFixed(1)}%</td><td>{x.age_minutes==null?'—':`${x.age_minutes.toFixed(0)} min`}</td><td>{x.unrealized_pnl==null?'—':`₹${x.unrealized_pnl.toLocaleString()}`}</td></tr>)}</tbody></table></div></section></div><section className="card"><h2>Strategy scorecard</h2><div className="table-wrap"><table><thead><tr><th>Strategy</th><th>Trades</th><th>Win rate</th><th>P&L</th></tr></thead><tbody>{(d.strategy_summary||[]).map((x:any)=><tr key={x.strategy}><td>{x.strategy}</td><td>{x.trades}</td><td>{x.win_rate==null?'—':`${(x.win_rate*100).toFixed(1)}%`}</td><td>₹{x.pnl.toLocaleString()}</td></tr>)}</tbody></table></div></section><section className="card"><h2>Captured trade excursions</h2><p className="muted">MFE/MAE is calculated from quote snapshots captured by this app during a trade's holding period. It improves as the app collects more history.</p><div className="table-wrap"><table><thead><tr><th>Security</th><th>Buy</th><th>Sell</th><th>Obs.</th><th>MFE %</th><th>MAE %</th></tr></thead><tbody>{(d.trade_excursions||[]).map((x:any,i:number)=><tr key={i}><td>{x.security}</td><td>{x.buy_date}</td><td>{x.sell_date}</td><td>{x.observations}</td><td>{x.mfe_pct==null?'—':`${(x.mfe_pct*100).toFixed(2)}%`}</td><td>{x.mae_pct==null?'—':`${(x.mae_pct*100).toFixed(2)}%`}</td></tr>)}</tbody></table></div></section></div>}