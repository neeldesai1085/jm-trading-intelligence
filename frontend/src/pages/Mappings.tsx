import{useEffect,useState}from'react';import{get,API}from'../lib/api';export default function Mappings(){const[rows,setRows]=useState<any[]>([]);const[isin,setIsin]=useState(''),[security,setSecurity]=useState(''),[provider,setProvider]=useState('upstox'),[key,setKey]=useState('');const load=()=>get<any[]>('/instrument-mappings').then(setRows);useEffect(()=>{void load()},[]);const save=async()=>{await fetch(API+'/instrument-mappings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({isin,security,provider,instrument_key:key})});setIsin('');setSecurity('');setKey('');load()};return <div className="page"><div className="eyebrow">MARKET DATA</div><h1>Instrument Mapping</h1><p className="muted">Map each ISIN to the exact provider instrument key.</p><div className="card form"><input placeholder="ISIN" value={isin} onChange={e=>setIsin(e.target.value)}/><input placeholder="Security" value={security} onChange={e=>setSecurity(e.target.value)}/><select value={provider} onChange={e=>setProvider(e.target.value)}><option>upstox</option><option>zerodha</option></select><input placeholder="Provider instrument key" value={key} onChange={e=>setKey(e.target.value)}/><button onClick={save}>Save mapping</button></div><div className="card"><table><thead><tr><th>ISIN</th><th>Security</th><th>Provider</th><th>Instrument Key</th></tr></thead><tbody>{rows.map(r=><tr key={r.isin}><td>{r.isin}</td><td>{r.security}</td><td>{r.provider}</td><td>{r.instrument_key}</td></tr>)}</tbody></table></div></div>}
