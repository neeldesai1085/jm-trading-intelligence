import {useState} from 'react';import {login} from '../lib/api';export default function Login({onAuthenticated,onCreateAccount}:{onAuthenticated:(u:any)=>void;onCreateAccount:()=>void}){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');return <div className="auth-shell"><form className="auth-card" onSubmit={async e=>{e.preventDefault();setError('');try{const r=await login(email,password);onAuthenticated(r)}catch(err:any){setError(err.message)}}}><div className="eyebrow">JM TRADING INTELLIGENCE</div><h1>Sign in</h1><input placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><button type="submit">Sign in</button>{error&&<p className="error">{error}</p>}<p className="auth-switch">Don't have an account? <button type="button" className="link-button" onClick={onCreateAccount}>Create account</button></p></form></div>}