import {useState} from 'react';
import {login} from '../lib/api';
export default function Login({onAuthenticated}:{onAuthenticated:(u:any)=>void}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[err,setErr]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();setBusy(true);setErr('');try{const r=await login(email,password);onAuthenticated(r.user)}catch(x:any){setErr(x.message||'Sign in failed')}finally{setBusy(false)}};return <div className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">JM TRADING INTELLIGENCE</div><h1>Sign in</h1><p className="muted">Access your private trading workspace.</p><label>Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{err&&<div className="alert high">{err}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><div className="auth-switch">Need an account? <button type="button" onClick={()=>onAuthenticated({showRegister:true})}>Create one</button></div></form></div>}
