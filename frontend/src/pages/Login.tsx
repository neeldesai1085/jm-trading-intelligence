import {useState} from 'react';import {login,setToken} from '../lib/api';export default function Login({appName,onLogin}:{appName:string,onLogin:(name:string,code:string)=>void}){const[name,setName]=useState('');const[code,setCode]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();setBusy(true);setError('');try{const r=await login(name.trim(),code);setToken(r.token);onLogin(r.member_name,code)}catch(err:any){setError(err.message||'Login failed')}finally{setBusy(false)}};return <div className="login-shell"><form className="login-card" onSubmit={submit}><div className="eyebrow">FAMILY TRADING WORKSPACE</div><h1>{appName}</h1><p className="muted">Private access for your household.</p><input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" required maxLength={40}/><input value={code} onChange={e=>setCode(e.target.value)} placeholder="Family access code" type="password" required/><button disabled={busy}>{busy?'Signing in…':'Sign in'}</button>{error&&<div className="form-error">{error}</div>}</form></div>}