import {useEffect,useState} from 'react';import {get} from '../lib/api';export default function ExcelParity(){const [d,setD]=useState<any>(null);useEffect(()=>{get('/excel-parity').then(setD).catch(()=>setD({}))},[]);return <section><h1>Excel Parity</h1><p>The 22-tab model is exposed as structured JSON for auditing and charting.</p>{d?<pre className="raw">{JSON.stringify(d,null,2)}</pre>:<p>Loading…</p>}</section>}
