export const API=import.meta.env.VITE_API_URL||'http://localhost:8000/api';export async function get<T>(path:string):Promise<T>{const r=await fetch(API+path);if(!r.ok)throw new Error(await r.text());return r.json()}export async function upload(files:File[]){const f=new FormData();files.forEach(x=>f.append('files',x));const r=await fetch(API+'/imports/upload',{method:'POST',body:f});if(!r.ok)throw new Error(await r.text());return r.json()}
