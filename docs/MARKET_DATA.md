# Market data

`mock` is deterministic and is the CI default. Upstox and Zerodha are supported through configurable tokens and OAuth/session exchange flows. Securities are mapped from source ISINs to provider-specific instrument keys before quote retrieval.
